<section class="hero">
    <a href="/list"> 🔙 Back to messages</a>
    <h1 class="title">{{message.from}} to <code>{{message.to}}</code> at {{message.timestamp}}</h1>
    <h3 class="subtitle">"{{message.text}}"</h3>
</section>
